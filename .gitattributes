# Shell scripts must stay LF so GitHub Actions Linux runners execute them.
.github/scripts/*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.cs text eol=lf

# Data files are newline-agnostic; keep default handling.
data/*.jsonl -text

# Binary / generated artifacts.
bin/** -text
obj/** -text